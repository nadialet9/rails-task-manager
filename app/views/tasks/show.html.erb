<h1><%= @task.title %></h1>
<%= @task.details %>

<br>
<br>
<br>

<%= link_to "Back to list", tasks_path %> 
<%= link_to "Edit ", edit_task_path(@task) %>
<%= link_to "Delete",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>